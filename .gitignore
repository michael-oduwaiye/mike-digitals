# Never commit real secrets
.env

# Dependencies (reinstalled via npm install, no need to store in git)
node_modules/

# Local order data (contains customer phone numbers — don't publish this)
# data/orders.json

# OS/editor junk
.DS_Store
*.log
